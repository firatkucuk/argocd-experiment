apiVersion: apps/v1	
kind: Deployment
metadata:
  name: experiment-argocd-deployment
  labels:
    app: experiment-argocd-deployment-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: experiment-argocd-deployment-app
  template:
    metadata:
      labels:
        app: experiment-argocd-deployment-app
    spec:
      containers:
      - name: experiment-argocd-deployment-app
        image:  ghcr.io/firatkucuk/experiment-argocd:c2a45e8e
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
